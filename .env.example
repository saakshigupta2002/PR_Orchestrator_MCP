# Example environment configuration for the PR Orchestrator MCP

# Set your personal access token with repo scope.  Required for all GitHub
# API operations.  Do not commit your real token to version control.
GITHUB_TOKEN=

# Username associated with the GitHub token.  Used when creating forks and
# constructing fork repository slugs.
GITHUB_USERNAME=saakshigupta2002

# E2B API key for provisioning isolated workspaces.  If empty, the
# orchestrator falls back to a local temporary directory (not recommended
# for production).  See docs/threat-model.md for security considerations.
E2B_API_KEY=

# Default safe mode for run_command.  Allowed values are "safe" or
# "expert".  Safe mode restricts git subcommands and other dangerous
# operations; expert mode relaxes these restrictions.
DEFAULT_SAFE_MODE=safe

# Maximum number of files that may be changed in a single patch.  Edits
# affecting more files will be rejected.
MAX_CHANGED_FILES=50

# Maximum total number of changed lines allowed in a patch.  Edits with
# more than this number of added or removed lines will be rejected.
MAX_PATCH_LINES=5000

# Default and maximum time-to-live (TTL) in minutes for workspaces.
RUN_TTL_MINUTES=60         # default TTL (1 hour)
RUN_TTL_MAX_MINUTES=360    # maximum TTL (6 hours)

# Allowed repositories that the orchestrator may operate on.  The list
# comprises comma-separated <owner>/<repo> slugs.  Wildcards (*) are
# supported within the repository name.  Only repositories listed here
# may be cloned or forked.
ALLOWED_REPOS=saakshigupta2002/repo1,saakshigupta2002/repo2